# `sync.Map`

Операции **`Store`**, **`Load`**, **`Range`**, **`Delete`**. Для учебного кода без конкурентных горутин; в бою `sync.Map` имеет смысл при типичных паттернах "много чтений, редкие записи по разным ключам".

```bash
cd lessons/10-sync-map
go run .
```
