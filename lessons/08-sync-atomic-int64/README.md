# Атомики: `atomic.Int64`

Счётчик из пакета **`sync/atomic`** (тип **`Int64`**, методы **`Add` / `Load`**). Требуется Go 1.19+.

```bash
cd lessons/08-sync-atomic-int64
go run -race .
```

Сравните с уроком **`09-sync-atomic-funcs`** (`AddInt64` на `int64`).
