# `sync.Mutex` и `map`

Тот же счётчик, что в уроке про гонку, но доступ к `map` защищён **`Lock` / `Unlock`**. Результат стабильно **`map[one:10]`**, `-race` чист.

```bash
cd lessons/06-sync-mutex
go run -race .
```

Ранее в репозитории дублировался файл `main_Mutex.go` с той же идеей - здесь один канонический пример.
