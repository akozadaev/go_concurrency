# Data race на `map`

Десять горутин без синхронизации делают `map["one"] += 1`. Обычная **`map` не потокобезопасна** - data race и неопределённое поведение.

```bash
cd lessons/05-sync-race
go run -race .
```

`-race` должен сообщить о гонках.
